pipeline {
    agent {
        label 'android'
    }
    stages {
        stage('Build App') {
            steps {
                // Run the app
                sh 'gradlew SocialProjectSCE'
            }
        }
        stage('Tests') {
            steps {
                // run the tests
               // sh 'gradlew test'
                echo 'Finished'
            }
        }
        stage('Finished') {
            steps {
                //print finished
                echo 'Finished'
            }
        }
    }
}
