pipeline {
    agent {
        label 'android'
    }
    stages {
        stage('Build App') {
            steps {
                // Run the app
                sh './gradlew :app:assembleDebug'
                sh './gradlew :app:assembleDebugAndroidTest'
                sh './gradlew SocialProjectSCE'
            }
        }
        stage('Tests') {
            steps {
                // run the tests
                sh './gradlew test'
            }
        }
        stage('Finished') {
            steps {
                //print finished
                echo 'Finished'
            }
        }
    }
}
