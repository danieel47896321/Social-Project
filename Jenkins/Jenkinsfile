pipeline {
    agent {
        label 'android'
    }
    stages {
        stage('Build App') {
            steps {
                // Clean and assemble APKs
                sh './gradlew clean assembleDebug assembleRelease'
            }
        }
        stage('Tests') {
            steps {
                // Clean and assemble APKs
                sh './gradlew test'
            }
        }
        stage('Finished') {
            steps {
                echo 'Finished'
            }
        }
    }
}
